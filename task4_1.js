/* 

Write a loop 1 to 200. Use break to exit the loop once you find 100.

*/

let numbers=0;
while(numbers < 200)
{
    console.log(numbers);
    if(numbers === 100)
    {
        break;
    }
    numbers++;
}